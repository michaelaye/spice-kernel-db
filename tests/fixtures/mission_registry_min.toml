# Minimal registry fixture for unit tests. Two entries:
# - LRO: two candidates (registry priority order)
# - MSL: one candidate

[LRO]
candidates = [
  "{server}{m}/data/spice/mk/",
  "{server}{m}/kernels/mk/",
]

[MSL]
candidates = [
  "{server}{m}/kernels/mk/former_versions/",
]
