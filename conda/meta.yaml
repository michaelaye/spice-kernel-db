{% set version = "0.19.0" %}

package:
  name: spice-kernel-db
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/s/spice-kernel-db/spice_kernel_db-{{ version }}.tar.gz
  sha256: 4ac011a5e64bbd4f07e0366ea2c2b8e3dc2a583326b7c66520734347b94f60f3

build:
  entry_points:
    - spice-kernel-db = spice_kernel_db.cli:main
    - skd = spice_kernel_db.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.11
    - hatchling
    - pip
  run:
    - python >=3.11
    - python-duckdb >=1.0
    - rich >=13.0

test:
  imports:
    - spice_kernel_db
  commands:
    - pip check
    - spice-kernel-db --help
  requires:
    - pip
    - python

about:
  home: https://github.com/michaelaye/spice-kernel-db
  summary: Browse, get, and manage SPICE kernels and metakernels across NASA and ESA mission archives
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - michaelaye
